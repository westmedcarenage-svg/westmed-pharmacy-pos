const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);$$('nav button').forEach(b=>b.onclick=()=>{$$('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');$$('.page').forEach(x=>x.classList.remove('active'));$('#'+b.dataset.page).classList.add('active');$('#page-title').textContent=b.textContent;if(b.dataset.page==='prescriptions')loadRx();if(b.dataset.page==='inventory')loadInventory();if(b.dataset.page==='migration')loadMigration();});async function getj(url,opt){const r=await fetch(url,opt);return r.json()}async function loadDashboard(){let d=await getj('/api/dashboard');$('#m-products').textContent=d.total_products;$('#m-rx').textContent=d.open_prescriptions;$('#m-exp').textContent=d.expiring_90;$('#m-low').textContent=d.low_stock}async function searchProducts(){let q=encodeURIComponent($('#product-search').value);let rows=await getj('/api/products?q='+q);$('#product-results').innerHTML=rows.map(r=>`<tr><td>${r.sku}</td><td>${r.name}</td><td>${r.stock}</td><td>$${Number(r.retail_price).toFixed(2)}</td><td><button>Add</button></td></tr>`).join('')}async function createRx(){let payload={store_id:Number($('#store').value),patient_name:$('#rx-patient').value,prescriber:$('#rx-prescriber').value,rx_number:$('#rx-no').value,drug_name:$('#rx-drug').value,quantity:Number($('#rx-qty').value||1),refills:Number($('#rx-refills').value||0),sig:$('#rx-sig').value,auxiliary_warning:$('#rx-warning').value};if(!payload.patient_name||!payload.drug_name||!payload.sig){alert('Patient, medicine and directions are required.');return}let d=await getj('/api/prescriptions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});$('#label-preview').innerHTML=`<b>WESTMED PHARMACY</b><br><b>${payload.patient_name}</b><br>${payload.drug_name}<br>${payload.sig}<br>Qty: ${payload.quantity} &nbsp; Rx: ${d.rx_number}<br>${payload.auxiliary_warning||''}<br><small>Prepared for pharmacist verification</small>`;loadRx();loadDashboard()}async function loadRx(){let rows=await getj('/api/prescriptions');$('#rx-list').innerHTML=rows.map(r=>`<div class="rx-card"><b>${r.rx_number} · ${r.patient_name}</b><small>${r.prescriber||'Prescriber not entered'} · ${r.status.replaceAll('_',' ')}</small>${r.status!=='ready'?`<div><button onclick="setRx(${r.id},'ready')">Pharmacist Check → Ready</button></div>`:''}</div>`).join('')||'<p>No prescriptions yet.</p>'}async function setRx(id,status){await getj(`/api/prescriptions/${id}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});loadRx();loadDashboard()}async function loadInventory(){let rows=await getj('/api/inventory');$('#inventory-list').innerHTML=rows.map(r=>`<tr><td>${r.store}</td><td>${r.sku}</td><td>${r.name}</td><td>${r.lot_number}</td><td>${r.expiry_date||''}</td><td>${r.quantity}</td></tr>`).join('')||'<tr><td colspan="6">No batch inventory imported yet.</td></tr>'}async function loadMigration(){let d=await getj('/api/migration/status');$('#migration-counts').innerHTML=Object.entries(d).map(([k,v])=>`<p><b>${k.replace('_',' ')}</b>: ${v}</p>`).join('')}async function importCsv(){let f=$('#mig-file').files[0];if(!f){alert('Choose a CSV file.');return}let fd=new FormData();fd.append('file',f);let entity=$('#mig-entity').value;let r=await fetch('/api/migration/import/'+entity,{method:'POST',body:fd});let d=await r.json();$('#mig-result').textContent=JSON.stringify(d,null,2);loadMigration()}async function loadAI(){let d=await getj('/api/ai/context');$('#ai-result').textContent=JSON.stringify(d,null,2)}loadDashboard();